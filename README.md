# Django_TodoList
A Django-based task management system with calendar and notification features. Includes custom Model Manager with expired_and_incomplete() method, Class-Based Views, and responsive UI.
